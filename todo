* add reset button to map, keep position on not-first load
* correct android page http://developer.android.com/resources/tutorials/views/hello-mapview.html:
  * custom class constructor -> call this(...)
  * call constructor with 2 params
* add pictures (add statically into bundle OR download dynamically, add app settings -> show images?)
* clear button to filterbox
* walking/driving route to bath
* static bath details, get on demand (not all info in main xml)
* favorites, store only refs to all, not whole bath objects (&id)

/* reload button in global menu
/* diversify error messages (maybe throw koffeinfreiexception, handle messages in throwing methods)
/* add about menu item
/* disable search box etc. when no data is loaded (-> hide search box, maybe display some other view anyway)
/* last modified is wrong
/* home button -> press state
/* back button (catch when not on overview, else default, test especially progress dialog!)
/* add license!!
/* strikethrough not up-to-date temperatures (older than ~2 days)
/* add uv index
/* map keys debug/build -> viewmodifier, set map view propery
/* rename: overview -> liste, overview -> map
/* if data is null, switch to tab 1 from overview map doesn't work properly
/* ERROR: add to favorites
/* text colors on detail view are not the defined ones!
/* bath details, i18n
/ * add opening hours
/* bug: randomly map tab is inactive!
/* button -> show bath in map (links to map overview, highlights the bath)
/* when reloading on favorites, doesn't switch to overview